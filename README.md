# OB03_virtual_zoo
Creation of the base class `Animal` that will contain common attributes (e.g. `name`, `age`) and methods (`make_sound()`, `eat()`) for all animals.
Implementation of inheritance by creating subclasses `Bird`, `Mammal`, and `Reptile` that inherit from the `Animal` class.

